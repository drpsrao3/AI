services:
  - type: web
    name: ai-law-app
    plan: starter  # or free if you implement memory optimizations
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers=1 --threads=4 --timeout=120 app:app
    env: python